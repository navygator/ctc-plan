- content_for :page_title do
  Редактировать грейд

.form-horizontal
  = form_for @grade do |f|
    = render partial: 'form', locals: { f: f }
    = f.submit "Сохранить", class: 'btn btn-primary pull-right'
.back-to-list
  = link_to 'Назад', grades_path, class: 'btn btn-primary'