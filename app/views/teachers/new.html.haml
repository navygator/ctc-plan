- content_for :page_title do
  Новый преподаватель

.form-horizontal
  = form_for @teacher do |f|
    = render partial: 'form', locals: { f: f }
    = f.submit "Сохранить", class: 'btn btn-primary pull-right'
.back-to-list
  = link_to 'Назад', teachers_path, class: 'btn btn-primary'