- content_for :page_title do
  Возрастные категории

%p= link_to 'Создать', new_age_category_path, class: 'btn btn-primary'
- if @age_categories.present?
  %table.table.table-striped
    %thead
      %tr
        %th Название
        %th.actions
    %tbody
      - @age_categories.each do |age_category|
        %tr
          %td= age_category.name
          %td.actions
            = link_to edit_age_category_path(age_category), class: "btn btn-default btn-round", title: "Редактировать" do
              %i.fa.fa-edit
            = link_to age_category_path(age_category), method: :delete, class: "btn btn-danger btn-round", title: "Удалить", data: { confirm: "Are you shure?" } do
              %i.fa.fa-trash-o