- content_for :page_title do
  Уровни достижений

%p= link_to 'Создать', new_level_path, class: 'btn btn-primary'
- if @levels.present?
  %table.table.table-striped
    %thead
      %tr
        %th Название
        %th.actions
    %tbody
      - @levels.each do |level|
        %tr
          %td= level.name
          %td.actions
            = link_to edit_level_path(level), class: "btn btn-default btn-round", title: "Редактировать" do
              %i.fa.fa-edit
            = link_to level_path(level), method: :delete, class: "btn btn-danger btn-round", title: "Удалить", data: { confirm: "Are you shure?" } do
              %i.fa.fa-trash-o